

### 3.1 问题定义

给定少量标注数据集 $\mathcal D_l=\{(X_i^l,H_i^l)\}_{i=1}^{N_l}$ 和大量无标注数据集 $\mathcal D_u=\{X_j^u\}_{j=1}^{N_u}$，其中 $X=\{L_1,L_2,L_3\}$ 表示三帧不同曝光的 LDR 输入序列，$L_2$ 为参考帧，$H$ 为目标 HDR 图像。本文目标是在少量成对标注数据和大量未标注动态多曝光序列的共同约束下，学习鲁棒的 HDR 重建模型，并从未标注样本中挖掘可靠的像素级伪监督信息。

与现有多曝光 HDR 方法一致，我们首先将每帧 LDR 图像映射到 HDR 辐照度域：
$$
R_r=\frac{L_r^\gamma}{t_r}, \quad r\in\{1,2,3\}, \quad \gamma=2.2,
$$
其中 $t_r$ 为第 $r$ 帧的曝光时间。随后将每帧 LDR 图像与其 HDR 域表示沿通道维拼接，得到：
$$
I_r=[L_r,R_r],
$$
并记网络输入为：
$$
Z=\{I_1,I_2,I_3\}.
$$
动态多曝光 HDR 重建同时受到极端曝光和跨曝光运动错位的影响，同一组 LDR 观测在饱和、遮挡和运动边界等区域可能对应多个合理的 HDR 解释。因此，仅预测单一中心 HDR 难以刻画局部重建歧义。为此，本文将 HDR 预测扩展为中心预测与像素级预测区间。对于输入 $Z$，网络输出：
$$
(\hat H_m,\hat H_U,\hat H_L)=F_\theta(Z),
$$
其中 $\hat H_m$ 表示中心 HDR 预测，$\hat H_U$ 与 $\hat H_L$ 分别表示预测区间的上界与下界。具体地，上下界由中心预测和上下两侧非负偏移量构造，偏移量通过 Softplus 激活保证非负，从而满足：
$$
\hat H_L\le \hat H_m\le \hat H_U.
$$
$F_\theta$ 表示添加区间预测头的 HDR 重建网络，网络细节见附录。

所有监督损失、误差度量与可靠性评估均在 $\mu$-law tone-mapped 域中计算：
$$
T(H)=\frac{\log(1+\mu H)}{\log(1+\mu)}.
$$
对于任意预测区间，我们定义其像素级区间宽度为：
$$
W(\hat H_U,\hat H_L)=T(\hat H_U)-T(\hat H_L).
$$
在本文中，$W$ 不仅表示预测区间的大小，也被进一步视为像素级重建风险的代理量。理想情况下，容易重建且伪标签可靠的区域应具有较窄的预测区间，而饱和、遮挡、运动错位或模型预测不稳定的区域应具有更宽的预测区间。然而，单纯依赖分位数回归学习到的区间宽度并不能天然保证其与真实重建误差一致。为此，后续章节将从有标签真实误差和无标签伪误差信号两个层面对区间宽度进行误差感知塑形，并进一步利用少量标注数据对区间宽度与真实误差之间的关系进行校准，从而将 $W$ 转化为更可靠的伪监督筛选依据。

整体训练采用 EMA teacher-student 框架。首先，student 在标注数据上进行误差感知的区间监督学习，使其同时获得中心重建能力和风险感知的区间预测能力。随后，以训练后的 student 初始化 EMA teacher，并由 teacher 为无标注样本生成中心伪标签、预测区间及其宽度图。对于无标注数据，本文进一步利用跨运动状态不一致性、可观测区域重投影残差和历史预测不稳定性构造伪误差信号，引导区间宽度在高风险区域变宽。最后，基于标注校准得到的可靠性阈值和跨运动状态兼容性验证，筛选可靠伪监督区域，并结合点-集合联合蒸馏优化 student，teacher 参数通过 EMA 持续更新。

### 3.2 误差感知的区间监督学习

在标注数据上，我们首先训练 student 以获得中心 HDR 重建能力和像素级预测区间。给定标注样本 $(Z^l,H^l)$，student 输出：
$$
(\hat H_m^l,\hat H_U^l,\hat H_L^l)=F_\theta(Z^l).
$$
其中 $\hat H_m^l$ 为中心 HDR 预测，$\hat H_U^l$ 与 $\hat H_L^l$ 分别为预测区间上界与下界。对应的区间宽度为：
$$
W^l=T(\hat H_U^l)-T(\hat H_L^l).
$$
传统分位数回归能够使上下界分别逼近目标分位点，从而形成具有统计意义的预测区间。然而，在动态多曝光 HDR 重建中，仅依赖分位数损失并不能显式保证区间宽度与中心预测误差之间具有良好相关性。特别是在饱和、遮挡和运动错位等高风险区域，模型可能产生过度自信的预测，即中心误差较大但区间宽度较小。为缓解该问题，本文在分位数区间学习的基础上进一步引入误差感知区间学习，使区间宽度不仅反映预测区间大小，也能够更好地表征像素级重建风险。

首先，我们采用中心重建损失训练中心预测分支：
$$
\mathcal L_{\mathrm{rec}}^l
=
\left\|T(\hat H_m^l)-T(H^l)\right\|_1.
$$
同时，采用分位数回归损失约束上下界：
$$
\mathcal L_q^l
=
\rho_{\alpha_q/2}\!\left(T(H^l)-T(\hat H_L^l)\right)
+
\rho_{1-\alpha_q/2}\!\left(T(H^l)-T(\hat H_U^l)\right),
$$
其中 $\rho_\tau(\cdot)$ 表示 pinball loss，$\alpha_q$ 为目标未覆盖率。该损失使下界和上界分别学习对应条件分位点，从而为预测区间提供基本的覆盖能力。

为了进一步增强区间宽度与真实误差之间的关系，我们定义中心预测误差为：
$$
e^l=
\left|
T(\hat H_m^l)-T(H^l)
\right|.
$$
若某个像素具有较大的中心误差，则其区间宽度不应过窄；反之，若误差较小，则模型可以给出较窄的预测区间。基于此，我们引入 failure-aware confidence penalty：
$$
\mathcal L_{\mathrm{conf}}^l
=
\frac{\mathrm{sg}(e^l)}{W^l+\epsilon}
+
\beta\log(W^l+\epsilon),
$$
其中 $\mathrm{sg}(\cdot)$ 表示 stop-gradient，$\epsilon$ 为数值稳定项，$\beta$ 控制区间紧致性约束的强度。第一项显式惩罚高误差但窄区间的过度自信预测，第二项避免模型通过无限增大区间宽度来降低损失。通过这种方式，区间宽度被鼓励与中心预测误差保持正相关，同时仍保持必要的紧致性。

然而，单次训练迭代中的误差图可能受到随机增强、局部噪声和优化状态的影响。为获得更稳定的像素级错误风险先验，我们在中心预测分支达到相对稳定后，统计一段历史训练阶段内的预测误差。对于第 $i$ 张标注图像，在 epoch $t_1$ 到 $t_2$ 之间收集中心预测误差图：
$$
E_i^{(t)}
=
\left|
T(\hat H_{m,i}^{(t)})-T(H_i^l)
\right|,
\quad t\in[t_1,t_2].
$$
随后对历史误差图取平均，得到该图像的平均误差图：
$$
\bar E_i^l
=
\frac{1}{K}
\sum_{t=t_1}^{t_2}
E_i^{(t)},
$$
其中 $K=t_2-t_1+1$。$\bar E_i^l$ 描述了当前模型族在该标注样本上长期难以重建的区域，可作为像素级错误风险的空间先验。

需要注意的是，我们并不强制区间宽度 $W_i^l$ 在数值上精确等于历史平均误差图 $\bar E_i^l$。由于区间宽度和预测误差分别承担不同的统计含义，直接进行逐像素数值拟合可能破坏预测区间的分位数语义。因此，本文仅约束二者在空间分布形态或相对排序上保持一致。具体地，我们采用排序一致性损失：
$$
\mathcal L_{\mathrm{rank}}^l
=
\frac{1}{|\mathcal P|}
\sum_{(p,q)\in\mathcal P}
\max
\left(
0,
m-
\mathrm{sign}(\bar E_p^l-\bar E_q^l)(W_p^l-W_q^l)
\right),
$$
其中 $\mathcal P$ 表示从图像中采样得到的像素对集合，$p$ 和 $q$ 表示像素位置，$m$ 为排序间隔。该损失鼓励历史平均误差更大的像素具有更宽的预测区间，即：
$$
\bar E_p^l>\bar E_q^l
\quad \Rightarrow \quad
W_p^l>W_q^l.
$$
通过这种相对排序约束，区间宽度能够学习真实误差的空间分布趋势，而不会被强制拟合误差图的绝对数值。

综上，有标签数据上的误差感知区间学习损失定义为：
$$
\mathcal L_{\mathrm{FAI}}^l
=
\mathcal L_{\mathrm{conf}}^l
+
\lambda_r
\mathcal L_{\mathrm{rank}}^l,
$$
即：
$$
\mathcal L_{\mathrm{FAI}}^l
=
\frac{\mathrm{sg}(e^l)}{W^l+\epsilon}
+
\beta\log(W^l+\epsilon)
+
\lambda_r
\mathcal L_{\mathrm{rank}}(W^l,\bar E^l),
$$
其中 $\lambda_r$ 控制误差分布排序约束的权重。

最终，有标签监督阶段的总体损失为：
$$
\mathcal L_{\mathrm{sup}}'
=
\mathcal L_{\mathrm{rec}}^l
+
\lambda_q\mathcal L_q^l
+
\lambda_{\mathrm{FAI}}^l
\mathcal L_{\mathrm{FAI}}^l,
$$
其中 $\lambda_q$ 和 $\lambda_{\mathrm{FAI}}^l$ 分别控制分位数区间学习和误差感知区间学习的权重。

该监督目标包含三重作用：中心重建损失保证模型的 HDR 重建能力，分位数损失提供预测区间的基本覆盖约束，而误差感知区间损失显式抑制高误差区域中的过度自信窄区间预测，并使区间宽度在空间分布上更接近真实错误风险。通过该设计，后续用于伪监督筛选的区间宽度 $W$ 不再仅由分位数回归隐式决定，而是在训练阶段被赋予更明确的误差风险语义。

### 3.3 误差引导的无标签区间塑形

在无标签数据上，由于缺少真实 HDR 参考图像，无法直接计算中心预测误差并监督区间宽度。然而，动态多曝光 HDR 重建中的高风险区域通常会在运动状态、观测一致性或训练历史上表现出不稳定性。例如，跨运动状态预测差异较大的区域往往对应运动错位、遮挡或对齐失败；在参考帧良好曝光区域中，若重建 HDR 无法解释输入 LDR 观测，则说明该区域的伪标签可能不可靠；而训练过程中长期预测不稳定的区域也往往具有较高重建风险。基于这一观察，本文在无标签数据上构造误差风险信号，用于引导 student 的区间宽度在潜在错误区域变宽，从而进一步抑制过度自信的伪监督。

给定无标签输入 $Z^u$，EMA teacher 在原始输入状态下输出：
$$
(\hat H_{m,o}^{t},\hat H_{U,o}^{t},\hat H_{L,o}^{t})=F_{\theta^t}(Z^u).
$$
同时，student 在原始输入状态下输出：
$$
(\hat H_{m,o}^{s},\hat H_{U,o}^{s},\hat H_{L,o}^{s})=F_{\theta^s}(Z^u),
$$
并定义 student 的原始状态区间宽度为：
$$
W_o^s=T(\hat H_{U,o}^{s})-T(\hat H_{L,o}^{s}).
$$
本文从两类无标签风险信号引导区间宽度学习。第一类是全局有效的预测不稳定性信号，包括跨运动状态不一致性和历史预测不稳定性，用于构造全图伪误差图；第二类是局部有效的观测一致性信号，即参考帧良好曝光区域中的可观测重投影不一致性。由于该观测一致性仅在参考帧良好曝光区域具有物理意义，本文不将其直接融合进全图伪误差图，而是作为单独的局部宽度惩罚项。

首先，为刻画跨曝光运动错位带来的不稳定性，我们使用预训练光流模型估计非参考帧到参考帧的运动场，并将非参考帧特征或输入图像 warp 至参考帧坐标系，得到运动补偿输入 $Z_{\mathrm{mc}}^u$。EMA teacher 在运动补偿状态下输出：
$$
(\hat H_{m,\mathrm{mc}}^{t},\hat H_{U,\mathrm{mc}}^{t},\hat H_{L,\mathrm{mc}}^{t})
=
F_{\theta^t}(Z_{\mathrm{mc}}^u).
$$
若同一无标签样本在原始状态和运动补偿状态下产生明显不同的 HDR 预测，则说明该区域对运动状态变化敏感，伪标签可信性较低。因此，我们定义跨状态不一致性为：
$$
D_{\mathrm{state}}
=
\left|
T(\hat H_{m,o}^{t})
-
T(\hat H_{m,\mathrm{mc}}^{t})
\right|.
$$
$D_{\mathrm{state}}$ 越大，表示该区域在不同运动状态下越不稳定，对应的区间宽度越不应过窄。

其次，为刻画训练过程中的预测稳定性，我们维护 EMA teacher 在历史训练阶段对无标签样本的预测记录。记 $\mathcal T_h$ 为历史 teacher checkpoint 或历史 EMA 状态集合，则历史预测不稳定性定义为：
$$
D_{\mathrm{hist}}
=
\mathrm{Var}_{k\in\mathcal T_h}
\left(
T(\hat H_{m,o}^{t,k})
\right),
$$
其中 $\hat H_{m,o}^{t,k}$ 表示第 $k$ 个历史 teacher 状态在原始输入状态下的中心 HDR 预测。若某一区域在训练过程中预测长期波动较大，则说明模型对该区域缺乏稳定解释，因此应赋予更宽的预测区间。

由于训练早期 teacher 预测尚未稳定，历史预测方差可能更多反映整体优化波动，而不一定对应真实困难区域。因此，本文对历史预测不稳定性采用逐步增强的权重：
$$
\omega_3(t)
=
\omega_3^{\max} r(t),
$$
其中 $r(t)$ 为随训练迭代逐渐增大的 ramp-up 函数。例如：
$$
r(t)=
\begin{cases}
0, & t<T_{\mathrm{warm}},\\
\frac{t-T_{\mathrm{warm}}}{T_{\mathrm{ramp}}}, & T_{\mathrm{warm}}\le t<T_{\mathrm{warm}}+T_{\mathrm{ramp}},\\
1, & t\ge T_{\mathrm{warm}}+T_{\mathrm{ramp}}.
\end{cases}
$$
通过该策略，训练早期主要依赖跨状态不一致性估计全图风险；随着 teacher 逐渐稳定，历史预测不稳定性被逐步引入，用于发现长期不确定区域。

基于跨运动状态不一致性和历史预测不稳定性，我们构造全图伪误差图：
$$
\tilde E_{\mathrm{global}}^u
=
\frac{
\omega_1\mathcal N(D_{\mathrm{state}})
+
\omega_3(t)\mathcal N(D_{\mathrm{hist}})
}{
\omega_1+\omega_3(t)+\epsilon
},
$$
其中 $\mathcal N(\cdot)$ 表示归一化操作，$\omega_1$ 和 $\omega_3(t)$ 分别为跨状态不一致性和历史预测不稳定性的权重。$\tilde E_{\mathrm{global}}^u$ 是全图范围内的潜在错误风险代理，其数值越大，表示该区域越可能受到运动状态变化或历史预测不稳定的影响，因此 student 的预测区间宽度应相应增大。

为了利用全图伪误差图引导无标签区间宽度学习，我们定义全局误差引导区间塑形损失：
$$
\mathcal L_{\mathrm{global}}^u
=
\frac{\mathrm{sg}(\tilde E_{\mathrm{global}}^u)}{W_o^s+\epsilon}
+
\beta_u\log(W_o^s+\epsilon)
+
\lambda_r^u
\mathcal L_{\mathrm{rank}}
(W_o^s,\tilde E_{\mathrm{global}}^u),
$$
其中 $\mathrm{sg}(\cdot)$ 表示 stop-gradient，避免伪误差信号被反向优化；$\beta_u$ 控制无标签区间紧致性约束的强度。第一项惩罚高伪误差但窄区间的预测，使跨状态不一致或历史不稳定区域获得更宽的预测区间；第二项防止 student 通过无限增大区间宽度来降低损失；第三项约束区间宽度与全图伪误差图在空间排序上保持一致。

其中，排序一致性损失定义为：
$$
\mathcal L_{\mathrm{rank}}
(W_o^s,\tilde E_{\mathrm{global}}^u)
=
\frac{1}{|\mathcal P_u|}
\sum_{(p,q)\in\mathcal P_u}
\max
\left(
0,
m-
\mathrm{sign}(\tilde E_{\mathrm{global},p}^u-\tilde E_{\mathrm{global},q}^u)
(W_{o,p}^s-W_{o,q}^s)
\right),
$$
其中 $\mathcal P_u$ 表示从无标签图像中采样得到的像素对集合。当某个像素的全图伪误差风险高于另一个像素时，该损失鼓励其对应的预测区间更宽。

接下来，本文在参考帧良好曝光区域中引入局部观测一致性宽度惩罚。由于重建 HDR 与参考帧 $L_2$ 对齐，观测一致性仅在参考帧坐标系下具有直接物理意义。因此，本文仅利用参考帧的良好曝光区域构造可观测重投影不一致性。参考帧在 HDR 辐照度域中的观测表示为：
$$
R_2=\frac{L_2^\gamma}{t_2}.
$$
我们定义参考帧良好曝光区域为：
$$
\Omega_2^{\mathrm{well}}
=
\mathbf 1(L_{\min}<L_2<L_{\max}),
$$
其中 $L_{\min}$ 和 $L_{\max}$ 分别为欠曝光与饱和阈值。对于该区域，teacher 预测的 HDR 应与参考帧观测保持一致。因此，可观测重投影不一致性定义为：
$$
D_{\mathrm{photo}}
=
\Omega_2^{\mathrm{well}}
\odot
\left|
T(\hat H_{m,o}^{t})
-
T(R_2)
\right|.
$$
当参考帧良好曝光区域中的重投影残差较大时，说明 teacher 预测的 HDR 难以解释输入中的可靠观测，该区域的伪标签风险较高。由于 $D_{\mathrm{photo}}$ 仅在 $\Omega_2^{\mathrm{well}}$ 内有效，本文不将其与全图伪误差图相加，而是仅在良好曝光区域中作为额外的局部宽度惩罚。

具体地，观测一致性宽度惩罚定义为：
$$
\mathcal L_{\mathrm{photo}}^u
=
\left\|
\Omega_2^{\mathrm{well}}
\odot
\left(
\frac{\mathrm{sg}(D_{\mathrm{photo}})}
{W_o^s+\epsilon}
+
\beta_{\mathrm{ph}}\log(W_o^s+\epsilon)
\right)
\right\|_1
+
\lambda_{r,\mathrm{ph}}^u
\mathcal L_{\mathrm{rank}}^{\Omega_2^{\mathrm{well}}}
(W_o^s,D_{\mathrm{photo}}),
$$
其中 $\beta_{\mathrm{ph}}$ 控制局部区间紧致性约束的强度，$\lambda_{r,\mathrm{ph}}^u$ 控制局部排序一致性约束的权重。$\mathcal L_{\mathrm{rank}}^{\Omega_2^{\mathrm{well}}}$ 表示仅在参考帧良好曝光区域内采样像素对的排序损失：
$$
\mathcal L_{\mathrm{rank}}^{\Omega_2^{\mathrm{well}}}
(W_o^s,D_{\mathrm{photo}})
=
\frac{1}{|\mathcal P_{\mathrm{ph}}|}
\sum_{(p,q)\in\mathcal P_{\mathrm{ph}}}
\max
\left(
0,
m-
\mathrm{sign}(D_{\mathrm{photo},p}-D_{\mathrm{photo},q})
(W_{o,p}^s-W_{o,q}^s)
\right),
$$
其中 $\mathcal P_{\mathrm{ph}}$ 表示从 $\Omega_2^{\mathrm{well}}$ 内采样得到的像素对集合。该损失鼓励参考帧良好曝光区域中重投影不一致性更大的像素具有更宽的预测区间。

最终，无标签误差引导的区间塑形损失定义为：
$$
\mathcal L_{\mathrm{FAI}}^u
=
\mathcal L_{\mathrm{global}}^u
+
\lambda_{\mathrm{ph}}
\mathcal L_{\mathrm{photo}}^u,
$$
其中 $\lambda_{\mathrm{ph}}$ 控制局部观测一致性宽度惩罚的权重。

该损失不直接监督 student 拟合 teacher 的中心伪标签，而是专门用于塑形 student 的区间宽度，使其在无标签高风险区域中避免过度自信。全局误差引导项利用跨状态不一致性和历史预测不稳定性在全图范围内估计潜在风险；局部观测一致性项仅在参考帧良好曝光区域中利用可靠 LDR 观测修正区间宽度。由于两类信号的有效区域和物理含义不同，本文将其分别建模，而不是强行相加为单一伪误差图。随后，经过误差塑形后的区间宽度将进一步在标注数据上进行校准，并用于构造可靠伪监督掩码。

### 3.4 标注校准的跨运动状态蒸馏

经过前述误差感知区间监督学习和无标签误差引导区间塑形后，区间宽度 $W$ 已被赋予更明确的像素级重建风险语义。然而，区间宽度仍然是由模型预测得到的内部量，不能直接视为绝对可靠性指标。因此，本文进一步利用少量标注数据对区间宽度与真实重建误差之间的关系进行后验校准，并在原始运动状态与运动补偿状态下共同验证伪标签的稳定性，从而为无标签样本筛选可靠伪监督区域。

#### 3.4.1 跨运动状态教师预测

给定无标签输入 $Z^u$，EMA teacher 在原始输入状态下输出：
$$
(\hat H_{m,o}^{t},\hat H_{U,o}^{t},\hat H_{L,o}^{t})
=
F_{\theta^t}(Z^u).
$$
对应的原始状态区间宽度为：
$$
W_o^t
=
T(\hat H_{U,o}^{t})
-
T(\hat H_{L,o}^{t}).
$$
为验证伪标签对跨曝光运动错位的稳定性，我们使用预训练光流模型估计非参考帧到参考帧的运动场，并将 $I_1$ 和 $I_3$ warp 至参考帧坐标系，得到运动补偿输入 $Z_{\mathrm{mc}}^u$。EMA teacher 在运动补偿状态下输出：
$$
(\hat H_{m,\mathrm{mc}}^{t},\hat H_{U,\mathrm{mc}}^{t},\hat H_{L,\mathrm{mc}}^{t})
=
F_{\theta^t}(Z_{\mathrm{mc}}^u).
$$
对应的运动补偿状态区间宽度为：
$$
W_{\mathrm{mc}}^t
=
T(\hat H_{U,\mathrm{mc}}^{t})
-
T(\hat H_{L,\mathrm{mc}}^{t}).
$$
运动补偿状态不被视为更可靠的监督来源，而仅用于跨状态可靠性验证。直观而言，若某个区域在原始状态和运动补偿状态下均具有较小区间宽度，并且两个状态给出的可行 HDR 解空间相互兼容，则该区域更可能对应稳定且可信的伪监督；反之，若预测结果对运动状态变化敏感，则该区域可能受到错位、遮挡或对齐误差影响，不应被赋予过强的伪监督约束。

#### 3.4.2 标注校准的可靠性筛选

虽然 FAI 通过真实误差和伪误差信号增强了区间宽度与错误风险之间的相关性，但模型预测的 $W$ 仍可能存在尺度偏差或局部过度自信。因此，本文利用少量标注样本对区间宽度进行后验校准，将其转化为由真实误差支持的可靠性筛选准则。

对于标注样本 $(Z^l,H^l)$，我们同样构造原始状态输入 $Z_o^l$ 和运动补偿状态输入 $Z_{\mathrm{mc}}^l$。记 $v\in{o,\mathrm{mc}}$ 表示原始状态或运动补偿状态。EMA teacher 在状态 $v$ 下输出：
$$
(\hat H_{m,v}^{t,l},\hat H_{U,v}^{t,l},\hat H_{L,v}^{t,l})
=
F_{\theta^t}(Z_v^l).
$$
对应的区间宽度为：
$$
W_v^l
=
T(\hat H_{U,v}^{t,l})
-
T(\hat H_{L,v}^{t,l}).
$$
在标注数据上，可以直接计算 teacher 中心预测与真实 HDR 之间的像素级误差：
$$
e_{v,ij}^l
=
\left|
T(\hat H_{m,v}^{t,l})_{ij}
-
T(H^l)_{ij}
\right|.
$$
我们将区间宽度 $W_v^l$ 视为候选可靠性指标，并统计在不同宽度阈值下被选中像素的经验纯度。状态 $v$ 下的像素级经验纯度定义为：
$$
\pi_{v,\mathrm{px}}(\tau)
=
\frac{
\sum_l\sum_{i,j}
\mathbf 1(W_{v,ij}^l\le\tau)
\mathbf 1(e_{v,ij}^l\le\epsilon_{\mathrm{px}})
}{
\sum_l\sum_{i,j}
\mathbf 1(W_{v,ij}^l\le\tau)
+\epsilon
}.
$$
其中 $\epsilon_{\mathrm{px}}$ 为可接受的像素级重建误差阈值。$\pi_{v,\mathrm{px}}(\tau)$ 表示在状态 $v$ 下，所有区间宽度不超过 $\tau$ 的像素中，真实误差小于 $\epsilon_{\mathrm{px}}$ 的比例。

随后，我们选择满足纯度约束的最大宽度阈值：
$$
\tau_v
=
\max
\left\{
\tau:
\pi_{v,\mathrm{px}}(\tau)
\ge
1-\alpha_{\mathrm{px}}
\right\},
$$
其中 $\alpha_{\mathrm{px}}$ 为允许的像素级错误比例。由此得到原始状态和运动补偿状态下的校准阈值：
$$
\tau_o,\quad \tau_{\mathrm{mc}}.
$$
该校准过程并不假设区间宽度与真实误差之间存在严格的一一对应关系，而是在标注数据上估计：
$$
P(e_v^l\le\epsilon_{\mathrm{px}}\mid W_v^l\le\tau_v)
\ge
1-\alpha_{\mathrm{px}}.
$$
因此，$W$ 被从模型内部的不确定性输出转化为经过标注误差支持的集合级可靠性指标。与前述 FAI 结合后，区间宽度一方面在训练阶段被误差风险信号塑形，另一方面在筛选阶段通过标注误差进行后验校准，从而降低高风险区域中过度自信伪标签被保留的概率。

#### 3.4.3 可靠掩码与点-集合联合蒸馏

对于无标签样本，我们利用校准阈值筛选在原始状态和运动补偿状态下均具有较低预测风险的像素，并进一步验证两个状态下预测解空间的兼容性。最终可靠掩码定义为：
$$
M
=
\mathbf 1(W_o^t\le \tau_o)
\mathbf 1(W_{\mathrm{mc}}^t\le \tau_{\mathrm{mc}})
\mathbf 1
\left(
T(\hat H_{L,\mathrm{mc}}^t)
\le
T(\hat H_{m,o}^t)
\le
T(\hat H_{U,\mathrm{mc}}^t)
\right).
$$
其中，前两项要求该像素在原始状态和运动补偿状态下均具有较小的校准区间宽度，表示其在两种运动状态下均被判定为低风险区域。最后一项要求原始状态下 teacher 的中心预测落入运动补偿状态 teacher 所给出的可行解空间中，即原始状态伪标签应与运动补偿状态下的预测区间兼容。该约束能够进一步过滤运动错位、遮挡和对齐失败区域中的不稳定伪监督。

在可靠区域 $M$ 上，本文采用点-集合联合蒸馏训练 student。点蒸馏直接利用 teacher 的中心预测提供明确优化方向：
$$
\mathcal L_{\mathrm{point}}
=
\left\|
M\odot
\left(
T(\hat H_{m,o}^{s})
-
T(\hat H_{m,o}^{t})
\right)
\right\|_1.
$$
然而，即使经过可靠性筛选，teacher 的中心伪标签仍可能存在残余误差。若 student 被强制拟合单一中心伪标签，可能会继承 teacher 的局部偏差。为此，我们进一步将 teacher 的预测区间视为可行解空间，仅当 student 的中心预测落在该区间之外时才施加惩罚：
$$
\mathcal L_{\mathrm{set}}
=
\left\|
M\odot
\left(
[T(\hat H_{L,o}^{t})-T(\hat H_{m,o}^{s})]_+
+
[T(\hat H_{m,o}^{s})-T(\hat H_{U,o}^{t})]_+
\right)
\right\|_1.
$$
其中 $[\cdot]_+=\max(\cdot,0)$。集合蒸馏为 student 保留了区间内的合理预测自由度，从而降低其对单一中心伪标签的过拟合风险。

为了进一步提升 student 对运动状态变化的鲁棒性，我们约束 student 在原始状态和运动补偿状态下的中心预测保持一致。给定运动补偿输入 $Z_{\mathrm{mc}}^u$，student 输出：
$$
(\hat H_{m,\mathrm{mc}}^{s},\hat H_{U,\mathrm{mc}}^{s},\hat H_{L,\mathrm{mc}}^{s})
=
F_{\theta^s}(Z_{\mathrm{mc}}^u).
$$
跨状态一致性损失定义为：
$$
\mathcal L_{\mathrm{cons}}
=
\left\|
M\odot
\left(
T(\hat H_{m,\mathrm{mc}}^{s})
-
\mathrm{sg}(T(\hat H_{m,o}^{s}))
\right)
\right\|_1.
$$
其中 $\mathrm{sg}(\cdot)$ 表示 stop-gradient。该损失鼓励 student 在不同运动状态输入下保持稳定预测，避免对运动补偿扰动过度敏感。

综上，无标签蒸馏损失定义为：
$$
\mathcal L_u'
=
\lambda_p \mathcal L_{\mathrm{point}}
+
\lambda_s \mathcal L_{\mathrm{set}}
+
\mathcal L_{\mathrm{cons}}
+
\lambda_{\mathrm{FAI}}^u
\mathcal L_{\mathrm{FAI}}^u,
$$
其中 $\lambda_p$ 和 $\lambda_s$ 分别控制点蒸馏与集合蒸馏的权重，$\lambda_{\mathrm{FAI}}^u$ 控制无标签误差引导区间塑形的权重。$\lambda_p$ 与 $\lambda_s$ 可随训练动态调整，使模型在训练早期更多依赖集合蒸馏保留预测自由度，在训练后期逐步增强点蒸馏以获得更明确的监督信号。

最终半监督训练目标为：
$$
\mathcal L_{\mathrm{semi}}
=
\mathcal L_{\mathrm{sup}}'
+
\lambda_u
\mathcal L_u',
$$
其中：
$$
\mathcal L_{\mathrm{sup}}'
=
\mathcal L_{\mathrm{rec}}^l
+
\lambda_q\mathcal L_q^l
+
\lambda_{\mathrm{FAI}}^l
\mathcal L_{\mathrm{FAI}}^l.
$$
通过上述设计，本文形成了“误差感知区间学习—标注校准可靠筛选—跨状态点-集合蒸馏”的完整半监督训练流程。误差感知区间学习使区间宽度更好地反映真实或潜在错误风险；标注校准将宽度转化为具有经验纯度约束的可靠性指标；跨状态验证进一步过滤运动不稳定区域；点-集合联合蒸馏则在利用可靠伪标签的同时，避免 student 过度拟合单一中心伪标签。